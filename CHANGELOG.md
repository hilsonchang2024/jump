# Changelog

## 0.1.0

- Initial release of the Windows `cmd` quick directory jump tool
- Added history-based fuzzy jump with recent-first matching
- Added `cmd` tracking for `cd`, `chdir`, `pushd`, and `popd`
- Added history listing and clean commands
